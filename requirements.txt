# Core dependencies
numpy>=1.21.0
dataclasses>=0.6
typing-extensions>=4.0.0

# Amazon Bedrock dependencies
boto3>=1.28.0
botocore>=1.31.0

# Async support
asyncio>=3.4.3

# Monitoring and performance
psutil>=5.8.0

# Production dependencies (optional)
redis>=4.0.0

# Development and testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
moto>=4.2.0  # For AWS mocking in tests

# Optional alternatives (not needed with Bedrock)
# anthropic>=0.3.0  # Direct Anthropic API (alternative to Bedrock)
# openai>=1.0.0  # OpenAI models (alternative)
# sentence-transformers>=2.0.0  # Local embeddings (alternative to Titan)